{
  "name": "trounce-flow-landing",
  "version": "1.0.0",
  "description": "Landing page for trounceflow project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "styles": "npx tailwindcss -i ./styles/main.css -o ./styles/output.css",
    "styles:watch": "npx tailwindcss -i ./styles/main.css -o ./styles/output.css -w"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.3"
  }
}
